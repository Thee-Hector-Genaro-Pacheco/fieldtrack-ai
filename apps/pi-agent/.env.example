# GPS Agent Mode (mock or hardware)
GPS_MODE=mock

# Hardware Serial Settings
GPS_SERIAL_PORT=/dev/ttyAMA0
GPS_BAUD_RATE=9600

# Telemetry Stale Threshold (in seconds)
GPS_STALE_AFTER_SECONDS=5

# Public Demo Mode (restricts coordinates to rounded values)
PUBLIC_DEMO_MODE=false
